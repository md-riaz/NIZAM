openapi: 3.1.0
info:
  title: NIZAM — Open Communications Control Platform API
  description: |
    Current API specification generated from the active Laravel route tree.
    Covers core routes and module-provided routes under /api/v1.
  version: 1.0.1
  contact:
    name: NIZAM Support
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://localhost:8091/api/v1
    description: Local development
  - url: https://api.nizam.example.com/api/v1
    description: Production

security:
  - bearerAuth: []

tags:
  - name: Authentication
  - name: Health
  - name: Tenants
  - name: Extensions
  - name: DIDs
  - name: Ring Groups
  - name: IVRs
  - name: Time Conditions
  - name: Call Routing Policies
  - name: Flows
  - name: Call Sessions
  - name: Calls
  - name: CDRs
  - name: Recordings
  - name: Webhooks
  - name: Call Events
  - name: Device Profiles
  - name: Gateways
  - name: Codec Metrics
  - name: Holiday Calendars
  - name: Schedules
  - name: Teams
  - name: Agents
  - name: Queues
  - name: Queue Metrics
  - name: Usage
  - name: Admin
  - name: Users
  - name: Audit Logs

paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      security: []
      operationId: healthCheck
      responses:
        '200':
          description: Healthy or degraded status
          content:
            application/json:
              schema:
                type: object

  /auth/register:
    post:
      tags: [Authentication]
      summary: Register a new user
      security: []
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: User registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '422':
          $ref: '#/components/responses/ValidationError'

  /auth/login:
    post:
      tags: [Authentication]
      summary: Login and receive bearer token
      security: []
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/logout:
    post:
      tags: [Authentication]
      summary: Logout current token
      operationId: logout
      responses:
        '200':
          description: Logged out

  /auth/me:
    get:
      tags: [Authentication]
      summary: Get current authenticated user
      operationId: me
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'

  /auth/tokens:
    get:
      tags: [Authentication]
      summary: List API tokens
      operationId: listTokens
      responses:
        '200':
          description: Token list
          content:
            application/json:
              schema:
                type: object
    post:
      tags: [Authentication]
      summary: Create API token
      operationId: createToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTokenRequest'
      responses:
        '201':
          description: Token created
          content:
            application/json:
              schema:
                type: object

  /auth/tokens/{tokenId}:
    parameters:
      - $ref: '#/components/parameters/TokenId'
    delete:
      tags: [Authentication]
      summary: Revoke API token
      operationId: revokeToken
      responses:
        '204':
          description: Token revoked

  /tenants:
    get:
      tags: [Tenants]
      summary: List tenants
      operationId: listTenants
      responses:
        '200':
          description: Tenant list
    post:
      tags: [Tenants]
      summary: Create tenant
      operationId: createTenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantInput'
      responses:
        '201':
          description: Tenant created

  /tenants/provision:
    post:
      tags: [Tenants]
      summary: Provision tenant with automated setup
      operationId: provisionTenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantProvisionRequest'
      responses:
        '201':
          description: Tenant provisioned

  /tenants/{tenantId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Tenants]
      summary: Get tenant
      operationId: getTenant
      responses:
        '200':
          description: Tenant details
    put:
      tags: [Tenants]
      summary: Update tenant
      operationId: updateTenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantInput'
      responses:
        '200':
          description: Tenant updated
    delete:
      tags: [Tenants]
      summary: Delete tenant
      operationId: deleteTenant
      responses:
        '204':
          description: Tenant deleted

  /tenants/{tenantId}/settings:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Tenants]
      summary: Get tenant settings
      operationId: getTenantSettings
      responses:
        '200':
          description: Settings response
    put:
      tags: [Tenants]
      summary: Merge-update tenant settings
      operationId: updateTenantSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                settings:
                  type: object
      responses:
        '200':
          description: Settings updated

  /admin/dashboard:
    get:
      tags: [Admin]
      summary: System-wide admin dashboard
      operationId: adminDashboard
      responses:
        '200':
          description: Dashboard data

  /admin/sip-profiles:
    get:
      tags: [Admin]
      summary: List SIP profiles
      operationId: listSipProfiles
      responses:
        '200':
          description: SIP profile list
    post:
      tags: [Admin]
      summary: Create SIP profile
      operationId: createSipProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipProfileInput'
      responses:
        '201':
          description: SIP profile created

  /admin/sip-profiles/{sipProfileId}:
    parameters:
      - name: sipProfileId
        in: path
        required: true
        schema: { type: integer }
    get:
      tags: [Admin]
      summary: Get SIP profile
      operationId: getSipProfile
      responses:
        '200':
          description: SIP profile details
    put:
      tags: [Admin]
      summary: Update SIP profile
      operationId: updateSipProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SipProfileInput'
      responses:
        '200':
          description: SIP profile updated
    delete:
      tags: [Admin]
      summary: Delete SIP profile
      operationId: deleteSipProfile
      responses:
        '204':
          description: SIP profile deleted

  /admin/blocked-destinations:
    get:
      tags: [Admin]
      summary: List blocked destinations
      operationId: listBlockedDestinations
      parameters:
        - name: tenant_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Blocked destination list
    post:
      tags: [Admin]
      summary: Create blocked destination
      operationId: createBlockedDestination
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlockedDestinationInput'
      responses:
        '201':
          description: Blocked destination created

  /admin/blocked-destinations/{blockedDestinationId}:
    parameters:
      - name: blockedDestinationId
        in: path
        required: true
        schema: { type: integer }
    get:
      tags: [Admin]
      summary: Get blocked destination
      operationId: getBlockedDestination
      responses:
        '200':
          description: Blocked destination details
    put:
      tags: [Admin]
      summary: Update blocked destination
      operationId: updateBlockedDestination
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlockedDestinationInput'
      responses:
        '200':
          description: Blocked destination updated
    delete:
      tags: [Admin]
      summary: Delete blocked destination
      operationId: deleteBlockedDestination
      responses:
        '204':
          description: Blocked destination deleted

  /users:
    get:
      tags: [Users]
      summary: List users
      operationId: listUsers
      responses:
        '200':
          description: User list
    post:
      tags: [Users]
      summary: Create user
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserInput'
      responses:
        '201':
          description: User created

  /users/{userId}:
    parameters:
      - $ref: '#/components/parameters/UserId'
    get:
      tags: [Users]
      summary: Get user
      operationId: getUser
      responses:
        '200':
          description: User details
    put:
      tags: [Users]
      summary: Update user
      operationId: updateUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserInput'
      responses:
        '200':
          description: User updated
    delete:
      tags: [Users]
      summary: Delete user
      operationId: deleteUser
      responses:
        '204':
          description: User deleted

  /users/{userId}/permissions:
    parameters:
      - $ref: '#/components/parameters/UserId'
    get:
      tags: [Users]
      summary: List user permissions
      operationId: listUserPermissions
      responses:
        '200':
          description: Permission list

  /users/{userId}/permissions/grant:
    parameters:
      - $ref: '#/components/parameters/UserId'
    post:
      tags: [Users]
      summary: Grant permissions to user
      operationId: grantUserPermissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PermissionMutationRequest'
      responses:
        '200':
          description: Permissions granted

  /users/{userId}/permissions/revoke:
    parameters:
      - $ref: '#/components/parameters/UserId'
    post:
      tags: [Users]
      summary: Revoke permissions from user
      operationId: revokeUserPermissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PermissionMutationRequest'
      responses:
        '200':
          description: Permissions revoked

  /permissions:
    get:
      tags: [Users]
      summary: List available permissions
      operationId: listAvailablePermissions
      responses:
        '200':
          description: Available permissions

  /tenants/{tenantId}/stats:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Tenants]
      summary: Get tenant stats
      operationId: tenantStats
      responses:
        '200':
          description: Tenant statistics

  /tenants/{tenantId}/usage/summary:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Usage]
      summary: Get usage summary
      operationId: usageSummary
      responses:
        '200':
          description: Usage summary

  /tenants/{tenantId}/usage/collect:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Usage]
      summary: Collect usage snapshot
      operationId: usageCollect
      responses:
        '200':
          description: Usage snapshot collected

  /tenants/{tenantId}/usage/reconcile:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Usage]
      summary: Reconcile usage against CDRs
      operationId: usageReconcile
      responses:
        '200':
          description: Usage reconciliation

  /tenants/{tenantId}/extensions:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Extensions]
      summary: List extensions
      operationId: listExtensions
      responses:
        '200':
          description: Extension list
    post:
      tags: [Extensions]
      summary: Create extension
      operationId: createExtension
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExtensionInput'
      responses:
        '201':
          description: Extension created

  /tenants/{tenantId}/extensions/{extensionId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/ExtensionId'
    get:
      tags: [Extensions]
      summary: Get extension
      operationId: getExtension
      responses:
        '200':
          description: Extension details
    put:
      tags: [Extensions]
      summary: Update extension
      operationId: updateExtension
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExtensionInput'
      responses:
        '200':
          description: Extension updated
    delete:
      tags: [Extensions]
      summary: Delete extension
      operationId: deleteExtension
      responses:
        '204':
          description: Extension deleted

  /tenants/{tenantId}/holiday-calendars:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Holiday Calendars]
      summary: List holiday calendars
      operationId: listHolidayCalendars
      responses:
        '200':
          description: Holiday calendar list
    post:
      tags: [Holiday Calendars]
      summary: Create holiday calendar
      operationId: createHolidayCalendar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HolidayCalendarInput'
      responses:
        '201':
          description: Holiday calendar created

  /tenants/{tenantId}/holiday-calendars/{holidayCalendarId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/HolidayCalendarId'
    get:
      tags: [Holiday Calendars]
      summary: Get holiday calendar
      operationId: getHolidayCalendar
      responses:
        '200':
          description: Holiday calendar details
    put:
      tags: [Holiday Calendars]
      summary: Update holiday calendar
      operationId: updateHolidayCalendar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HolidayCalendarInput'
      responses:
        '200':
          description: Holiday calendar updated
    delete:
      tags: [Holiday Calendars]
      summary: Delete holiday calendar
      operationId: deleteHolidayCalendar
      responses:
        '204':
          description: Holiday calendar deleted

  /tenants/{tenantId}/schedules:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Schedules]
      summary: List schedules
      operationId: listSchedules
      responses:
        '200':
          description: Schedule list
    post:
      tags: [Schedules]
      summary: Create schedule
      operationId: createSchedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleInput'
      responses:
        '201':
          description: Schedule created

  /tenants/{tenantId}/schedules/{scheduleId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/ScheduleId'
    get:
      tags: [Schedules]
      summary: Get schedule
      operationId: getSchedule
      responses:
        '200':
          description: Schedule details
    put:
      tags: [Schedules]
      summary: Update schedule
      operationId: updateSchedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleInput'
      responses:
        '200':
          description: Schedule updated
    delete:
      tags: [Schedules]
      summary: Delete schedule
      operationId: deleteSchedule
      responses:
        '204':
          description: Schedule deleted

  /tenants/{tenantId}/teams:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Teams]
      summary: List teams
      operationId: listTeams
      responses:
        '200':
          description: Team list
    post:
      tags: [Teams]
      summary: Create team
      operationId: createTeam
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamInput'
      responses:
        '201':
          description: Team created

  /tenants/{tenantId}/teams/{teamId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/TeamId'
    get:
      tags: [Teams]
      summary: Get team
      operationId: getTeam
      responses:
        '200':
          description: Team details
    put:
      tags: [Teams]
      summary: Update team
      operationId: updateTeam
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamInput'
      responses:
        '200':
          description: Team updated
    delete:
      tags: [Teams]
      summary: Delete team
      operationId: deleteTeam
      responses:
        '204':
          description: Team deleted

  /tenants/{tenantId}/flows:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Flows]
      summary: List flows
      operationId: listFlows
      responses:
        '200':
          description: Flow list
    post:
      tags: [Flows]
      summary: Create flow
      operationId: createFlow
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowInput'
      responses:
        '201':
          description: Flow created

  /tenants/{tenantId}/flows/{flowId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/FlowId'
    get:
      tags: [Flows]
      summary: Get flow
      operationId: getFlow
      responses:
        '200':
          description: Flow details
    put:
      tags: [Flows]
      summary: Update flow
      operationId: updateFlow
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowInput'
      responses:
        '200':
          description: Flow updated
    delete:
      tags: [Flows]
      summary: Delete flow
      operationId: deleteFlow
      responses:
        '204':
          description: Flow deleted

  /tenants/{tenantId}/flows/{flowId}/publish:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/FlowId'
    post:
      tags: [Flows]
      summary: Publish latest flow version
      operationId: publishFlow
      responses:
        '200':
          description: Flow published

  /tenants/{tenantId}/calls:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Call Sessions]
      summary: List call sessions
      operationId: listCallSessions
      responses:
        '200':
          description: Call session list

  /tenants/{tenantId}/calls/{callSessionId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/CallSessionId'
    get:
      tags: [Call Sessions]
      summary: Get call session
      operationId: getCallSession
      responses:
        '200':
          description: Call session details

  /tenants/{tenantId}/calls/{callSessionId}/analyze:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/CallSessionId'
    get:
      tags: [Call Sessions]
      summary: Analyze call session trace
      operationId: analyzeCallSession
      responses:
        '200':
          description: Call session analysis

  /tenants/{tenantId}/cdrs/export:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: Export CDRs (Simple)
      operationId: exportCdrs
      responses:
        '200':
          description: CSV/JSON export stream
    post:
      tags: [CDRs]
      summary: Advanced CDR Export
      description: Export filtered CDRs in CSV or JSON format.
      operationId: exportCdrsAdvanced
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                format: { type: string, enum: [csv, json], default: csv }
                filters: { type: object }
      responses:
        '200':
          description: CDR export file

  /tenants/{tenantId}/cdrs:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: List CDRs with advanced filtering
      operationId: listCdrs
      parameters:
        - name: q
          in: query
          description: Full-text search on caller, destination, UUID
          schema: { type: string }
        - name: call_type
          in: query
          schema: { type: string, enum: [inbound, outbound, internal, emergency] }
        - name: quality_score_min
          in: query
          schema: { type: number, minimum: 0, maximum: 5 }
        - name: direction
          in: query
          schema: { type: string, enum: [inbound, outbound] }
        - name: start_date
          in: query
          schema: { type: string, format: date-time }
        - name: end_date
          in: query
          schema: { type: string, format: date-time }
      responses:
        '200':
          description: CDR list with pagination and enrichment data

  /tenants/{tenantId}/cdrs/analytics/summary:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: Get CDR summary analytics (ASR, ACD)
      operationId: cdrAnalyticsSummary
      responses:
        '200':
          description: Summary stats
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CdrAnalyticsSummary'

  /tenants/{tenantId}/cdrs/analytics/volume:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: Get call volume trends
      operationId: cdrAnalyticsVolume
      responses:
        '200':
          description: Volume trends over time
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CdrAnalyticsVolume'

  /tenants/{tenantId}/cdrs/analytics/quality:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: Get quality trends (MOS, Packet Loss)
      operationId: cdrAnalyticsQuality
      responses:
        '200':
          description: Quality metrics trends
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CdrAnalyticsQuality'

  /tenants/{tenantId}/cdrs/analytics/destinations:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [CDRs]
      summary: Get top call destinations
      operationId: cdrAnalyticsDestinations
      responses:
        '200':
          description: Top destinations list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CdrAnalyticsDestinations'

  /tenants/{tenantId}/cdrs/{cdrId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/CdrId'
    get:
      tags: [CDRs]
      summary: Get detailed CDR with enrichment data
      operationId: getCdr
      responses:
        '200':
          description: CDR details including quality metrics and geolocation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CallDetailRecord'

  /tenants/{tenantId}/call-routing-policies:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Call Routing Policies]
      summary: List call routing policies
      operationId: listCallRoutingPolicies
      responses:
        '200':
          description: Policy list
    post:
      tags: [Call Routing Policies]
      summary: Create call routing policy
      operationId: createCallRoutingPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CallRoutingPolicyInput'
      responses:
        '201':
          description: Policy created

  /tenants/{tenantId}/call-routing-policies/{policyId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/PolicyId'
    get:
      tags: [Call Routing Policies]
      summary: Get call routing policy
      operationId: getCallRoutingPolicy
      responses:
        '200':
          description: Policy details
    put:
      tags: [Call Routing Policies]
      summary: Update call routing policy
      operationId: updateCallRoutingPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CallRoutingPolicyInput'
      responses:
        '200':
          description: Policy updated
    delete:
      tags: [Call Routing Policies]
      summary: Delete call routing policy
      operationId: deleteCallRoutingPolicy
      responses:
        '204':
          description: Policy deleted

  /tenants/{tenantId}/call-routing-policies/{policyId}/evaluate:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/PolicyId'
    post:
      tags: [Call Routing Policies]
      summary: Evaluate call routing policy
      operationId: evaluateCallRoutingPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicyEvaluationRequest'
      responses:
        '200':
          description: Evaluation result

  /tenants/{tenantId}/dids:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [DIDs]
      summary: List DIDs
      operationId: listDids
      responses:
        '200':
          description: DID list
    post:
      tags: [DIDs]
      summary: Create DID
      operationId: createDid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DidInput'
      responses:
        '201':
          description: DID created

  /tenants/{tenantId}/dids/{didId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/DidId'
    get:
      tags: [DIDs]
      summary: Get DID
      operationId: getDid
      responses:
        '200':
          description: DID details
    put:
      tags: [DIDs]
      summary: Update DID
      operationId: updateDid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DidInput'
      responses:
        '200':
          description: DID updated
    delete:
      tags: [DIDs]
      summary: Delete DID
      operationId: deleteDid
      responses:
        '204':
          description: DID deleted

  /tenants/{tenantId}/ring-groups:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Ring Groups]
      summary: List ring groups
      operationId: listRingGroups
      responses:
        '200':
          description: Ring group list
    post:
      tags: [Ring Groups]
      summary: Create ring group
      operationId: createRingGroup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RingGroupInput'
      responses:
        '201':
          description: Ring group created

  /tenants/{tenantId}/ring-groups/{ringGroupId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/RingGroupId'
    get:
      tags: [Ring Groups]
      summary: Get ring group
      operationId: getRingGroup
      responses:
        '200':
          description: Ring group details
    put:
      tags: [Ring Groups]
      summary: Update ring group
      operationId: updateRingGroup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RingGroupInput'
      responses:
        '200':
          description: Ring group updated
    delete:
      tags: [Ring Groups]
      summary: Delete ring group
      operationId: deleteRingGroup
      responses:
        '204':
          description: Ring group deleted

  /tenants/{tenantId}/ivrs:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [IVRs]
      summary: List IVRs
      operationId: listIvrs
      responses:
        '200':
          description: IVR list
    post:
      tags: [IVRs]
      summary: Create IVR
      operationId: createIvr
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IvrInput'
      responses:
        '201':
          description: IVR created

  /tenants/{tenantId}/ivrs/{ivrId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/IvrId'
    get:
      tags: [IVRs]
      summary: Get IVR
      operationId: getIvr
      responses:
        '200':
          description: IVR details
    put:
      tags: [IVRs]
      summary: Update IVR
      operationId: updateIvr
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IvrInput'
      responses:
        '200':
          description: IVR updated
    delete:
      tags: [IVRs]
      summary: Delete IVR
      operationId: deleteIvr
      responses:
        '204':
          description: IVR deleted

  /tenants/{tenantId}/time-conditions:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Time Conditions]
      summary: List time conditions
      operationId: listTimeConditions
      responses:
        '200':
          description: Time condition list
    post:
      tags: [Time Conditions]
      summary: Create time condition
      operationId: createTimeCondition
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimeConditionInput'
      responses:
        '201':
          description: Time condition created

  /tenants/{tenantId}/time-conditions/{timeConditionId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/TimeConditionId'
    get:
      tags: [Time Conditions]
      summary: Get time condition
      operationId: getTimeCondition
      responses:
        '200':
          description: Time condition details
    put:
      tags: [Time Conditions]
      summary: Update time condition
      operationId: updateTimeCondition
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimeConditionInput'
      responses:
        '200':
          description: Time condition updated
    delete:
      tags: [Time Conditions]
      summary: Delete time condition
      operationId: deleteTimeCondition
      responses:
        '204':
          description: Time condition deleted

  /tenants/{tenantId}/webhooks:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Webhooks]
      summary: List webhooks
      operationId: listWebhooks
      responses:
        '200':
          description: Webhook list
    post:
      tags: [Webhooks]
      summary: Create webhook
      operationId: createWebhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookInput'
      responses:
        '201':
          description: Webhook created

  /tenants/{tenantId}/webhooks/{webhookId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/WebhookId'
    get:
      tags: [Webhooks]
      summary: Get webhook
      operationId: getWebhook
      responses:
        '200':
          description: Webhook details
    put:
      tags: [Webhooks]
      summary: Update webhook
      operationId: updateWebhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookInput'
      responses:
        '200':
          description: Webhook updated
    delete:
      tags: [Webhooks]
      summary: Delete webhook
      operationId: deleteWebhook
      responses:
        '204':
          description: Webhook deleted

  /tenants/{tenantId}/webhooks/{webhookId}/delivery-attempts:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/WebhookId'
    get:
      tags: [Webhooks]
      summary: List webhook delivery attempts
      operationId: listWebhookDeliveryAttempts
      responses:
        '200':
          description: Delivery attempt list

  /tenants/{tenantId}/webhooks/{webhookId}/delivery-stats:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/WebhookId'
    get:
      tags: [Webhooks]
      summary: Get webhook delivery stats
      operationId: getWebhookDeliveryStats
      responses:
        '200':
          description: Delivery statistics

  /tenants/{tenantId}/call-events:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Call Events]
      summary: List call events
      operationId: listCallEvents
      responses:
        '200':
          description: Call event list

  /tenants/{tenantId}/call-events/stream:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Call Events]
      summary: Stream call events via SSE
      operationId: streamCallEvents
      responses:
        '200':
          description: SSE stream
          content:
            text/event-stream:
              schema:
                type: string

  /tenants/{tenantId}/call-events/{callUuid}/trace:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/CallUuid'
    get:
      tags: [Call Events]
      summary: Get trace for call UUID
      operationId: traceCallEvents
      responses:
        '200':
          description: Call trace

  /tenants/{tenantId}/call-events/replay/{eventId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/EventId'
    get:
      tags: [Call Events]
      summary: Replay stored call event
      operationId: replayCallEvent
      responses:
        '200':
          description: Event payload

  /tenants/{tenantId}/call-events/redispatch/{eventId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/EventId'
    post:
      tags: [Call Events]
      summary: Redispatch stored event to matching webhooks
      operationId: redispatchCallEvent
      responses:
        '200':
          description: Redispatch result

  /tenants/{tenantId}/calls/originate:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Calls]
      summary: Originate a call
      operationId: originateCall
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OriginateCallRequest'
      responses:
        '200':
          description: Call initiated

  /tenants/{tenantId}/calls/status:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Calls]
      summary: Get active call status
      operationId: callStatus
      responses:
        '200':
          description: Call status

  /tenants/{tenantId}/calls/hangup:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Calls]
      summary: Hang up a call
      operationId: hangupCall
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HangupCallRequest'
      responses:
        '200':
          description: Call terminated

  /tenants/{tenantId}/calls/transfer:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Calls]
      summary: Transfer a call
      operationId: transferCall
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransferCallRequest'
      responses:
        '200':
          description: Call transferred

  /tenants/{tenantId}/calls/recording:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Calls]
      summary: Start or stop call recording
      operationId: toggleCallRecording
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CallRecordingRequest'
      responses:
        '200':
          description: Recording action applied

  /tenants/{tenantId}/calls/hold:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    post:
      tags: [Calls]
      summary: Hold or unhold a call
      operationId: holdCall
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldCallRequest'
      responses:
        '200':
          description: Hold action applied

  /tenants/{tenantId}/recordings:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Recordings]
      summary: List recordings
      operationId: listRecordings
      responses:
        '200':
          description: Recording list

  /tenants/{tenantId}/recordings/{recordingId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/RecordingId'
    get:
      tags: [Recordings]
      summary: Get recording metadata
      operationId: getRecording
      responses:
        '200':
          description: Recording details
    delete:
      tags: [Recordings]
      summary: Delete recording
      operationId: deleteRecording
      responses:
        '204':
          description: Recording deleted

  /tenants/{tenantId}/recordings/{recordingId}/download:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/RecordingId'
    get:
      tags: [Recordings]
      summary: Download recording file
      operationId: downloadRecording
      responses:
        '200':
          description: Recording file stream

  /tenants/{tenantId}/device-profiles:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Device Profiles]
      summary: List device profiles
      operationId: listDeviceProfiles
      responses:
        '200':
          description: Device profile list
    post:
      tags: [Device Profiles]
      summary: Create device profile
      operationId: createDeviceProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceProfileInput'
      responses:
        '201':
          description: Device profile created

  /tenants/{tenantId}/device-profiles/{deviceProfileId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/DeviceProfileId'
    get:
      tags: [Device Profiles]
      summary: Get device profile
      operationId: getDeviceProfile
      responses:
        '200':
          description: Device profile details
    put:
      tags: [Device Profiles]
      summary: Update device profile
      operationId: updateDeviceProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceProfileInput'
      responses:
        '200':
          description: Device profile updated
    delete:
      tags: [Device Profiles]
      summary: Delete device profile
      operationId: deleteDeviceProfile
      responses:
        '204':
          description: Device profile deleted

  /tenants/{tenantId}/gateways:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Gateways]
      summary: List gateways
      operationId: listGateways
      responses:
        '200':
          description: Gateway list
    post:
      tags: [Gateways]
      summary: Create gateway
      operationId: createGateway
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GatewayInput'
      responses:
        '201':
          description: Gateway created

  /tenants/{tenantId}/gateways/{gatewayId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/GatewayId'
    get:
      tags: [Gateways]
      summary: Get gateway
      operationId: getGateway
      responses:
        '200':
          description: Gateway details
    put:
      tags: [Gateways]
      summary: Update gateway
      operationId: updateGateway
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GatewayInput'
      responses:
        '200':
          description: Gateway updated
    delete:
      tags: [Gateways]
      summary: Delete gateway
      operationId: deleteGateway
      responses:
        '204':
          description: Gateway deleted

  /tenants/{tenantId}/codec-metrics:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Codec Metrics]
      summary: Get codec metrics
      operationId: codecMetrics
      responses:
        '200':
          description: Codec metrics

  /tenants/{tenantId}/agents:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Agents]
      summary: List agents
      operationId: listAgents
      responses:
        '200':
          description: Agent list
    post:
      tags: [Agents]
      summary: Create agent
      operationId: createAgent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInput'
      responses:
        '201':
          description: Agent created

  /tenants/{tenantId}/agents/{agentId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/AgentId'
    get:
      tags: [Agents]
      summary: Get agent
      operationId: getAgent
      responses:
        '200':
          description: Agent details
    put:
      tags: [Agents]
      summary: Update agent
      operationId: updateAgent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentInput'
      responses:
        '200':
          description: Agent updated
    delete:
      tags: [Agents]
      summary: Delete agent
      operationId: deleteAgent
      responses:
        '204':
          description: Agent deleted

  /tenants/{tenantId}/agents/{agentId}/state:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/AgentId'
    post:
      tags: [Agents]
      summary: Change agent state
      operationId: changeAgentState
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentStateChangeRequest'
      responses:
        '200':
          description: Agent state changed

  /tenants/{tenantId}/queues:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Queues]
      summary: List queues
      operationId: listQueues
      responses:
        '200':
          description: Queue list
    post:
      tags: [Queues]
      summary: Create queue
      operationId: createQueue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QueueInput'
      responses:
        '201':
          description: Queue created

  /tenants/{tenantId}/queues/{queueId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
    get:
      tags: [Queues]
      summary: Get queue
      operationId: getQueue
      responses:
        '200':
          description: Queue details
    put:
      tags: [Queues]
      summary: Update queue
      operationId: updateQueue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QueueInput'
      responses:
        '200':
          description: Queue updated
    delete:
      tags: [Queues]
      summary: Delete queue
      operationId: deleteQueue
      responses:
        '204':
          description: Queue deleted

  /tenants/{tenantId}/queues/{queueId}/members:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
    get:
      tags: [Queues]
      summary: List queue members
      operationId: listQueueMembers
      responses:
        '200':
          description: Queue members
    post:
      tags: [Queues]
      summary: Add queue member
      operationId: addQueueMember
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QueueMemberAddRequest'
      responses:
        '200':
          description: Queue member added

  /tenants/{tenantId}/queues/{queueId}/members/{agentId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
      - $ref: '#/components/parameters/AgentId'
    delete:
      tags: [Queues]
      summary: Remove queue member
      operationId: removeQueueMember
      responses:
        '204':
          description: Queue member removed

  /tenants/{tenantId}/queues/{queueId}/metrics/realtime:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
    get:
      tags: [Queue Metrics]
      summary: Get real-time queue metrics
      operationId: realtimeQueueMetrics
      responses:
        '200':
          description: Realtime queue metrics

  /tenants/{tenantId}/queues/{queueId}/metrics/aggregate:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
    post:
      tags: [Queue Metrics]
      summary: Aggregate queue metrics
      operationId: aggregateQueueMetrics
      responses:
        '200':
          description: Aggregated queue metrics

  /tenants/{tenantId}/queues/{queueId}/metrics/history:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/QueueId'
    get:
      tags: [Queue Metrics]
      summary: Queue metrics history
      operationId: historyQueueMetrics
      responses:
        '200':
          description: Queue metric history

  /tenants/{tenantId}/wallboard:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Queue Metrics]
      summary: Wallboard data for tenant queues
      operationId: wallboard
      responses:
        '200':
          description: Wallboard metrics

  /tenants/{tenantId}/agent-states:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Queue Metrics]
      summary: Agent state overview
      operationId: agentStates
      responses:
        '200':
          description: Agent states

  /tenants/{tenantId}/audit-logs:
    parameters:
      - $ref: '#/components/parameters/TenantId'
    get:
      tags: [Audit Logs]
      summary: List audit logs
      operationId: listAuditLogs
      responses:
        '200':
          description: Audit log list

  /tenants/{tenantId}/audit-logs/{auditLogId}:
    parameters:
      - $ref: '#/components/parameters/TenantId'
      - $ref: '#/components/parameters/AuditLogId'
    get:
      tags: [Audit Logs]
      summary: Get audit log
      operationId: getAuditLog
      responses:
        '200':
          description: Audit log details

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Token

  parameters:
    TenantId: { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
    TokenId: { name: tokenId, in: path, required: true, schema: { type: integer } }
    UserId: { name: userId, in: path, required: true, schema: { type: integer } }
    ExtensionId: { name: extensionId, in: path, required: true, schema: { type: string, format: uuid } }
    HolidayCalendarId: { name: holidayCalendarId, in: path, required: true, schema: { type: string, format: uuid } }
    ScheduleId: { name: scheduleId, in: path, required: true, schema: { type: string, format: uuid } }
    TeamId: { name: teamId, in: path, required: true, schema: { type: string, format: uuid } }
    FlowId: { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
    CallSessionId: { name: callSessionId, in: path, required: true, schema: { type: string, format: uuid } }
    CdrId: { name: cdrId, in: path, required: true, schema: { type: string, format: uuid } }
    PolicyId: { name: policyId, in: path, required: true, schema: { type: string, format: uuid } }
    DidId: { name: didId, in: path, required: true, schema: { type: string, format: uuid } }
    RingGroupId: { name: ringGroupId, in: path, required: true, schema: { type: string, format: uuid } }
    IvrId: { name: ivrId, in: path, required: true, schema: { type: string, format: uuid } }
    TimeConditionId: { name: timeConditionId, in: path, required: true, schema: { type: string, format: uuid } }
    WebhookId: { name: webhookId, in: path, required: true, schema: { type: string, format: uuid } }
    CallUuid: { name: callUuid, in: path, required: true, schema: { type: string } }
    EventId: { name: eventId, in: path, required: true, schema: { type: string, format: uuid } }
    RecordingId: { name: recordingId, in: path, required: true, schema: { type: string, format: uuid } }
    DeviceProfileId: { name: deviceProfileId, in: path, required: true, schema: { type: string, format: uuid } }
    GatewayId: { name: gatewayId, in: path, required: true, schema: { type: string, format: uuid } }
    AgentId: { name: agentId, in: path, required: true, schema: { type: string, format: uuid } }
    QueueId: { name: queueId, in: path, required: true, schema: { type: string, format: uuid } }
    AuditLogId: { name: auditLogId, in: path, required: true, schema: { type: string, format: uuid } }

  responses:
    Unauthorized:
      description: Authentication required
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Permission denied
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ValidationError:
      description: Validation failed
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
              errors:
                type: object
                additionalProperties:
                  type: array
                  items:
                    type: string

  schemas:
    ErrorResponse:
      type: object
      properties:
        message:
          type: string

    AuthResponse:
      type: object
      properties:
        user:
          $ref: '#/components/schemas/User'
        token:
          type: string

    User:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
        email: { type: string, format: email }
        role: { type: string }
        tenant_id: { type: string, format: uuid, nullable: true }

    RegisterRequest:
      type: object
      required: [name, email, password, password_confirmation]
      properties:
        name: { type: string }
        email: { type: string, format: email }
        password: { type: string, format: password }
        password_confirmation: { type: string, format: password }

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string, format: password }

    CreateTokenRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        abilities:
          type: array
          items: { type: string }

    TenantInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        domain: { type: string }
        slug: { type: string }
        max_extensions: { type: integer }
        max_concurrent_calls: { type: integer }
        max_dids: { type: integer }
        max_ring_groups: { type: integer }
        is_active: { type: boolean }
        settings: { type: object }

    TenantProvisionRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        domain: { type: string }
        slug: { type: string }
        max_extensions: { type: integer }
        max_concurrent_calls: { type: integer }
        max_dids: { type: integer }
        max_ring_groups: { type: integer }

    UserInput:
      type: object
      properties:
        name: { type: string }
        email: { type: string, format: email }
        password: { type: string, format: password }
        role: { type: string }
        tenant_id: { type: string, format: uuid, nullable: true }

    PermissionMutationRequest:
      type: object
      required: [permissions]
      properties:
        permissions:
          type: array
          items: { type: string }

    ExtensionInput:
      type: object
      required: [extension, password]
      properties:
        extension: { type: string }
        password: { type: string }
        directory_first_name: { type: string }
        directory_last_name: { type: string }
        effective_caller_id_name: { type: string }
        effective_caller_id_number: { type: string }
        outbound_caller_id_name: { type: string }
        outbound_caller_id_number: { type: string }
        voicemail_enabled: { type: boolean }
        voicemail_pin: { type: string }
        is_active: { type: boolean }

    HolidayCalendarInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        is_active: { type: boolean }

    ScheduleInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        timezone: { type: string }
        rules:
          type: array
          items: { type: object }
        exceptions:
          type: array
          items: { type: object }
        breaks:
          type: array
          items: { type: object }
        is_active: { type: boolean }

    TeamInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        is_active: { type: boolean }
        members:
          type: array
          items: { type: object }

    FlowInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        nodes:
          type: array
          items: { type: object }
        edges:
          type: array
          items: { type: object }
        is_active: { type: boolean }

    CallRoutingPolicyInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        conditions:
          type: array
          items: { type: object }
        match_destination_type: { type: string }
        match_destination_id: { type: string, format: uuid }
        no_match_destination_type: { type: string }
        no_match_destination_id: { type: string, format: uuid }
        priority: { type: integer }
        is_active: { type: boolean }

    PolicyEvaluationRequest:
      type: object
      properties:
        did: { type: string }
        caller_id: { type: string }
        time: { type: string, format: date-time }
        metadata: { type: object }

    DidInput:
      type: object
      required: [number]
      properties:
        number: { type: string }
        destination_type:
          type: string
          enum: [extension, ring_group, ivr, voicemail, time_condition, call_routing_policy, flow]
        destination_id: { type: string, format: uuid }
        description: { type: string }
        is_active: { type: boolean }

    RingGroupInput:
      type: object
      required: [name, strategy]
      properties:
        name: { type: string }
        strategy: { type: string, enum: [simultaneous, sequential] }
        members:
          type: array
          items: { type: string, format: uuid }
        ring_timeout: { type: integer }
        timeout_destination_type: { type: string }
        timeout_destination_id: { type: string, format: uuid }
        is_active: { type: boolean }

    IvrInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        greet_long: { type: string }
        greet_short: { type: string }
        options: { type: object }
        timeout: { type: integer }
        max_failures: { type: integer }
        timeout_destination_type: { type: string }
        timeout_destination_id: { type: string, format: uuid }
        is_active: { type: boolean }

    TimeConditionInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        rules:
          type: array
          items: { type: object }
        match_destination_type: { type: string }
        match_destination_id: { type: string, format: uuid }
        no_match_destination_type: { type: string }
        no_match_destination_id: { type: string, format: uuid }
        is_active: { type: boolean }

    WebhookInput:
      type: object
      required: [url, events, secret]
      properties:
        url: { type: string, format: uri, description: "The endpoint URL that will receive webhook POST requests." }
        events:
          type: array
          description: |
            List of event types this webhook subscribes to.
            ESL-sourced call lifecycle events: call.created, call.answered, call.ended, call.completed.
            Application events: call.started, call.bridge, call.missed, call.hangup, voicemail.received, registration.registered, registration.unregistered.
          items:
            type: string
            enum:
              - call.created
              - call.answered
              - call.ended
              - call.completed
              - call.started
              - call.bridge
              - call.missed
              - call.hangup
              - voicemail.received
              - registration.registered
              - registration.unregistered
        secret: { type: string, description: "Shared secret used for HMAC-SHA256 signature verification." }
        is_active: { type: boolean }

    OriginateCallRequest:
      type: object
      required: [extension, destination]
      properties:
        extension: { type: string }
        destination: { type: string }

    HangupCallRequest:
      type: object
      required: [uuid]
      properties:
        uuid: { type: string }
        cause: { type: string }

    TransferCallRequest:
      type: object
      required: [uuid, destination]
      properties:
        uuid: { type: string }
        destination: { type: string }
        leg: { type: string, enum: [aleg, bleg, both] }

    CallRecordingRequest:
      type: object
      required: [uuid, action]
      properties:
        uuid: { type: string }
        action: { type: string, enum: [start, stop] }

    HoldCallRequest:
      type: object
      required: [uuid, action]
      properties:
        uuid: { type: string }
        action: { type: string, enum: [hold, unhold] }

    DeviceProfileInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        vendor: { type: string }
        model: { type: string }
        mac_address: { type: string }
        config: { type: object }
        is_active: { type: boolean }

    GatewayInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        host: { type: string }
        port: { type: integer }
        username: { type: string }
        password: { type: string }
        transport: { type: string }
        register: { type: boolean }
        is_active: { type: boolean }

    AgentInput:
      type: object
      required: [name, extension_id]
      properties:
        name: { type: string }
        extension_id: { type: string, format: uuid }
        role: { type: string }
        is_active: { type: boolean }

    AgentStateChangeRequest:
      type: object
      required: [state]
      properties:
        state: { type: string }
        note: { type: string }

    QueueInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        strategy: { type: string }
        max_wait_time: { type: integer }
        service_level_threshold: { type: integer }
        music_on_hold: { type: string }
        is_active: { type: boolean }

    QueueMemberAddRequest:
      type: object
      required: [agent_id]
      properties:
        agent_id: { type: string, format: uuid }
        weight: { type: integer }
        paused: { type: boolean }

    CallDetailRecord:
      type: object
      properties:
        id: { type: string, format: uuid }
        uuid: { type: string }
        caller_id_name: { type: string }
        caller_id_number: { type: string }
        destination_number: { type: string }
        context: { type: string }
        start_stamp: { type: string, format: date-time }
        answer_stamp: { type: string, format: date-time, nullable: true }
        end_stamp: { type: string, format: date-time }
        duration: { type: integer }
        billsec: { type: integer }
        hangup_cause: { type: string }
        direction: { type: string, enum: [inbound, outbound] }
        call_type: { type: string, enum: [inbound, outbound, internal, emergency] }
        quality_score: { type: number, format: float, minimum: 0, maximum: 5 }
        mos: { type: number, format: float }
        packet_loss: { type: number, format: float }
        jitter: { type: integer }
        latency: { type: integer }
        sip_user_agent: { type: string }
        remote_media_ip: { type: string }
        tags: { type: array, items: { type: string } }
        metadata: { type: object }
        enrichment:
          $ref: '#/components/schemas/CdrEnrichment'

    CdrEnrichment:
      type: object
      properties:
        country_code: { type: string }
        country_name: { type: string }
        carrier_name: { type: string }
        number_type: { type: string }
        is_mobile: { type: boolean }
        raw_data: { type: object }

    CdrAnalyticsSummary:
      type: object
      properties:
        total_calls: { type: integer }
        answered_calls: { type: integer }
        missed_calls: { type: integer }
        total_duration: { type: integer }
        total_billsec: { type: integer }
        asr: { type: number, format: float }
        acd: { type: number, format: float }

    CdrAnalyticsVolume:
      type: object
      properties:
        labels: { type: array, items: { type: string } }
        data: { type: array, items: { type: integer } }

    CdrAnalyticsQuality:
      type: object
      properties:
        labels: { type: array, items: { type: string } }
        mos: { type: array, items: { type: number } }
        packet_loss: { type: array, items: { type: number } }

    CdrAnalyticsDestinations:
      type: array
      items:
        type: object
        properties:
          destination: { type: string }
          calls: { type: integer }
          duration: { type: integer }

    SipProfileInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        settings: { type: object }
        is_active: { type: boolean }

    BlockedDestinationInput:
      type: object
      required: [pattern]
      properties:
        tenant_id: { type: string, format: uuid, nullable: true }
        pattern: { type: string }
        description: { type: string }
